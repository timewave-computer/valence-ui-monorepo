export const SandboxConfig = {
  // NOTE: dynamic import paths must be string literals, so the path to the stories directory is hardcoded instead of configured
  storyFileRegex: /\.stories\.tsx$/, // regex to match story files
};
